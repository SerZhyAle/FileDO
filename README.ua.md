# FileDO - Розширені інструменти для файлів і сховищ

<div align="center">

[![Go Report Card](https://goreportcard.com/badge/github.com/SerZhyAle/FileDO)](https://goreportcard.com/report/github.com/SerZhyAle/FileDO)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![Version](https://img.shields.io/badge/Version-v2607301014-blue.svg)](https://github.com/SerZhyAle/FileDO)
[![Windows](https://img.shields.io/badge/Platform-Windows-lightgrey.svg)](https://github.com/SerZhyAle/FileDO)

**Тестування сховищ • Аналіз продуктивності • Безпечне видалення • Виявлення підробної місткості • Управління дублікатами**

*Консольна програма для носіїв Windows: тести ємності та швидкості, виявлення підробленої ємності, безпечне очищення, заповнення та робота з дублікатами.*

</div>

---

## Швидкий старт

### Найчастіші завдання

```bash
# Перевірити підробку USB/SD карти
filedo E: test del

# Тест продуктивності диска
filedo C: speed 100

# Безпечне очищення вільного місця
filedo D: fill 1000 del

# Пошук та управління дублікатами
filedo C: check-duplicates
filedo D: cd old del

# Копіювання з відстеженням прогресу
filedo folder C:\Source copy D:\Backup
filedo device E: copy F:\Archive
# Спершу порахувати дерево заради точного часу, що лишився (інакше копіювання починається з першого файлу)
filedo folder C:\Source copy D:\Backup --precount

# Швидке очищення папок
filedo folder C:\Temp wipe
filedo folder D:\Cache w

# Показати інформацію про диск
filedo C: info
```

### Порівняння тек і очищення

```bash
# Порівняти дві теки і зберегти звіт
filedo compare D:\Data E:\Backup

# Порівняти і видалити (перманентно, без підтвердження)
filedo cmp D:\Data E:\Backup del source  # видалити в Source, якщо є в Target
filedo cmp D:\Data E:\Backup del target  # видалити в Target, якщо є в Source
filedo cmp D:\Data E:\Backup del old     # видалити старіший (mtime), рівні: пропустити
filedo cmp D:\Data E:\Backup del new     # видалити новіший (mtime), рівні: пропустити
filedo cmp D:\Data E:\Backup del small   # видалити менший, рівні: пропустити
filedo cmp D:\Data E:\Backup del big     # видалити більший, рівні: пропустити
 
# Необов'язковий бік (обмеження за стороною)
filedo cmp D:\Data E:\Backup del small source  # тільки якщо менший на Source
filedo cmp D:\Data E:\Backup del big target    # тільки якщо більший на Target
filedo cmp D:\Data E:\Backup del old target    # тільки якщо старіший на Target
filedo cmp D:\Data E:\Backup del new source    # тільки якщо новіший на Source
```

Примітки: зіставлення за відносним шляхом; рівність - тільки за розміром; mtime для old/new; у Windows без урахування регістру; логи: compare_report_*.log, delete_report_<mode>_*.log.

### Встановлення

#### Варіант 1 - winget (рекомендовано)

```powershell
winget install SerZhyAle.FileDO
```

Встановлює інструменти командного рядка (`filedo`, `filedo_check`, `filedo_fill`, `filedo_test`) та графічне вікно `filedo_win` (сторінка на кожне завдання), і додає їх у `PATH`.

Також є в [Microsoft Store](https://apps.microsoft.com/detail/9PH1LPCMRG83) - ті самі інструменти, встановлення й оновлення бере на себе Store.

#### Варіант 2 - Інсталятор (setup EXE)

Завантажте `FileDO-<версія>-setup.exe` з [релізів](https://github.com/SerZhyAle/FileDO/releases/latest) і запустіть. Саме цей варіант робить FileDO звичайною програмою Windows, а не текою з виконуваними файлами:

- ставить файли в `C:\Program Files\FileDO` і додає `filedo` до системного `PATH`;
- створює **пункт у меню «Пуск»** та **піктограму на робочому столі** для вікна FileDO (`filedo_win.exe`);
- реєструє **інтеграцію з Провідником**: групу `File DO..` у контекстному меню будь-якого файлу - Secure (лишити, видалити чи затерти оригінал, або випадкова назва контейнера), Unsecure (за бажанням видалити контейнер або одразу запустити відновлений файл), Wipe this file, Check this file, Info - і тип документа `.fd-sec` із власною піктограмою: подвійний клік - це рівно пункт Unsecure and start: консоль запитує пароль, відновлює оригінал під його справжнім ім'ям у `%LOCALAPPDATA%\FileDO\reveal` - теку, доступну лише цьому обліковому запису та системі, - передає тій програмі, якій належить його справжнє розширення, і прибирає цю копію, коли вікно консолі закривається. У Windows 11 група - у «Показати додаткові параметри».

Інсталятор не має цифрового підпису, тому під час першого запуску Windows може показати «Windows захистила ваш ПК», а потім запросити права адміністратора. Звірте SHA256 (файл `.sha256` опубліковано поруч із завантаженням; `certutil -hashfile FileDO-<версія>-setup.exe SHA256`), потім виберіть «Докладніше» та «Усе одно запустити». Чому попередження з'являється, навіщо потрібні права адміністратора і чого FileDO ніколи не робить: [Windows попередила про FileDO](https://serzhyale.github.io/FileDO/guides/install-trust.html).

Останні два - компоненти, які можна зняти на сторінці «Customize» інсталятора й увімкнути або вимкнути пізніше через **«Змінити»** в «Програми та можливості». Для розгортання без запитань:

```powershell
# усе одразу, мовчки
FileDO-<версія>-setup.exe /quiet
# ..із журналом, якщо потім доведеться пояснювати результат
FileDO-<версія>-setup.exe /quiet /log install.log
# прибрати назад
FileDO-<версія>-setup.exe /uninstall
```

У тому ж релізі публікується і голий `FileDO-<версія>-windows-x64.msi` - саме цей файл лежить усередині setup EXE - для засобів розгортання, яким потрібен пакет та імена компонентів безпосередньо:

```powershell
msiexec /i FileDO-<версія>-windows-x64.msi /qn ADDLOCAL=Main,ExplorerIntegration,DesktopShortcut
msiexec /i FileDO-<версія>-windows-x64.msi /qn ADDLOCAL=Main   # без пунктів Провідника і піктограми
```

Видалення програми прибирає все, що записав інсталятор, включно із записами реєстру.

#### Варіант 3 - Microsoft Store (MSIX)

Один пакет, два входи: плитка **FileDO** - графічний інтерфейс зі сторінкою на кожне завдання, чия сторінка «Команда» збере й запустить будь-яку команду, і команда `filedo` у `PATH` для будь-якого терміналу. У збірці зі Store пунктів Провідника **немає**: пакетна збірка отримує їх лише через підписаний обробник оболонки, а це окрема робота.

#### Варіант 4 - Ручне завантаження

1. **Завантажте** `FileDO-<версія>-windows-x64.zip` з [релізів](https://github.com/SerZhyAle/FileDO/releases/latest) та розпакуйте куди завгодно.
2. **GUI**: в архіві є `filedo_win.exe` - запускайте поруч з `filedo.exe` для графічного вікна (сторінка на кожне завдання і сторінка «Команда»).
3. **Запуск**: з командного рядка або через GUI.

#### Інтеграція з Провідником без інсталятора

winget, переносний архів і `go install` не запускають інсталятор, тому не реєструють нічого. Ту саму групу й той самий тип документа можна попросити самому - і забрати назад так само:

```powershell
filedo fdsec register              # для цього користувача
filedo fdsec register -all-users   # для всієї машини (потрібна консоль з правами адміністратора)
filedo fdsec unregister            # прибирає рівно те, що було записано
```

FileDO видаляє лише позначене як своє: тип документа, який тим часом почав належати іншій програмі, лишається недоторканим, а той, хто володів `.fd-sec` до FileDO, отримує його назад. Машину, де вже є реєстрація інсталятора, команда теж не чіпає - копія для користувача показувала б кожен пункт двічі.

---

## Основні операції

<table>
<tr>
<td width="50%">

### Тестування пристроїв
```bash
# Інформація
filedo C: info
filedo D: short

# Виявлення підробної місткості
filedo E: test
filedo F: test del

# Тест продуктивності
filedo C: speed 100
filedo D: speed max
```

</td>
<td width="50%">

### Операції з файлами та папками
```bash
# Аналіз папки
filedo C:\temp info
filedo . short

# Тест продуктивності
filedo C:\data speed 100
filedo folder . speed max

# Мережеві операції
filedo \\server\share test
filedo network \\nas\backup speed 100

# Пакетні операції
filedo from commands.txt
filedo batch script.lst

# Очищення
filedo C:\temp clean
```

</td>
</tr>
</table>

---

## Секретні файли (`.fd-sec`)

Один файл іде в один контейнер під паролем і повертається назад - із командного рядка, з меню Провідника
або зі сторінок групи **Захист** у вікні. Справжнє ім'я оригіналу, його реальний розмір і позначки часу
запечатані всередині; сам контейнер видає лише власний розмір, своє видиме ім'я та позначки часу
(`rename` робить безіменний блок даних).

```bash
# Запакувати (пароль запитується двічі, без відлуння)
filedo report.docx secure

# Запакувати й позбутися оригіналу - з можливістю відновлення і складніше
filedo report.docx secure del p:hunter2
filedo report.docx secure wipe p:hunter2

# Повернути під запечатаним іменем або в поточну теку
filedo report.fd-sec unsecure
filedo report.fd-sec unsecure here

# Відкрити в потрібній програмі, не розпаковуючи
filedo report.fd-sec reveal
```

П'ять речей сказано прямо, бо функція безпеки, яка себе переоцінює, гірша за жодну:

- **Порожній пароль - це лише маскування, без таємниці.** Його приймають, і кожна поверхня, яка бере
  пароль, каже, чого він вартий, просто під час введення.
- **`wipe` знижує шанси на відновлення і нічого не обіцяє.** На SSD і на файлових системах із копіюванням
  під час запису чи журналюванням затирання на місці не гарантує, що старі блоки зникли.
- **Розкрита копія живе в `%LOCALAPPDATA%\FileDO\reveal`**, лише для читання і лише для цього облікового
  запису та системи. Вона зникає, коли ви скажете, або коли її відпустить програма, що її відкрила.
- **`unsecure start` - тобто подвійний клік - кладе свою копію в ту саму захищену теку**, а не поруч
  із контейнером, і прибирає її, коли закривається вікно консолі. Ця копія - ваш власний файл, а не
  версія лише для читання; якщо програма ще тримає її відкритою в цю мить, копію прибере наступний
  запуск FileDO. Назавжди файл повертає звичайний `unsecure`.
- **Після збою живлення ця копія залишиться на диску до наступного запуску FileDO**, який її прибирає.
  Більше її не прибере ніщо.

Ключа відновлення немає: забутий пароль - це втрачений файл. Оригінал залишається на місці, доки ви не
попросите його прибрати, і нічого не видаляється до того, як контейнер записано, перечитано й перевірено.
Програми та скрипти ніколи не запускаються з контейнера - їх видобувають і показують їхнє місце.

У вікні група **Захист** несе ті самі три операції у вигляді сторінок: пароль прихований, під час
пакування вводиться двічі й передається `filedo.exe` непомітно - він не потрапляє ні в командний рядок,
ні у звіт про запуск, ні у файл історії. Подвійний клік на `.fd-sec` це вікно не відкриває - він виконує
**Unsecure and start** у консолі, так само як однойменний пункт меню Провідника.

---

## Ключові можливості

### **Виявлення підробної місткості**
- **Тест на 100 файлів** по 1% місткості кожен
- **Випадкова позиційна перевірка** - кожен файл перевіряється в унікальних випадкових позиціях
- **Захист від складних підробок** - перемагає контролери, що зберігають дані в передбачуваних позиціях
- **Читабельні шаблони** - використовує `ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789` для легкого виявлення пошкоджень
- **Швидке сирове зондування** (`probe`) - 32 маркери через прямий доступ до LBA, ~1 хв (потрібен Адмін)

### **Тест продуктивності**
- Вимірювання реальної швидкості читання/запису
- Оптимізований стрімінг для великих файлів
- Відстеження прогресу з розрахунком ETA
- Налаштовувані розміри файлів (1МБ до 10ГБ)

### **Управління дублікатами файлів**
- **Вбудоване виявлення дублікатів** - інтегровано в основну програму
- **Кілька режимів вибору** (найстарший/найновіший/алфавітний)
- **Гнучкі дії** (видалити/перемістити дублікати)
- **Надійна ідентифікація на основі MD5**
- **Кешування хешів** для швидших повторних сканувань
- **Збереження/завантаження списків дублікатів** для пакетної обробки
- **Модульна архітектура** з виділеним пакетом fileduplicates

### **Функції безпеки**
- **Високошвидкісне безпечне видалення даних** для запобігання відновлення
- **Операції заповнення** з оптимізованим управлінням буферами
- **Пакетна обробка** для декількох цілей
- **Комплексна історія операцій** з JSON журналюванням
- **Контекстне переривання** - підтримка елегантного скасування

---

## Довідник команд

### Типи цілей (Автоматичне визначення)
| Шаблон | Тип | Приклад |
|--------|-----|---------|
| `C:`, `D:` | Пристрій | `filedo C: test` |
| `C:\folder` | Папка | `filedo C:\temp speed 100` |
| `\\server\share` | Мережа | `filedo \\nas\backup test` |
| `file.txt` | Файл | `filedo document.pdf info` |

### Операції
| Команда | Призначення | Приклад |
|---------|-------------|---------|
| `info` | Показати детальну інформацію | `filedo C: info` |
| `short` | Короткий зведений звіт | `filedo D: short` |
| `test` | Виявлення підробної місткості | `filedo E: test del` |
| `test N` | Тест з N файлами (за замовчуванням 100) | `filedo D: test 1000` |
| `probe` | Швидке зондування через сирий I/O (~1 хв, потрібен Адмін) | `filedo D: probe` |
| `speed <розмір>` | Тест продуктивності | `filedo C: speed 500` |
| `fill [розмір]` | Заповнити тестовими даними | `filedo D: fill 1000` |
| `clean` | Видалити тестові файли | `filedo C: clean` |
| `check-duplicates` | Знайти дублікати файлів | `filedo C: check-duplicates` |
| `cd [режим] [дія]` | Перевірити дублікати (коротка форма) | `filedo C: cd old del` |
| `from <файл>` | Виконати пакетні команди | `filedo from script.txt` |
| `hist` | Показати історію операцій | `filedo hist` |

### Модифікатори
| Прапор | Призначення | Приклад |
|--------|-------------|---------|
| `del` | Автовидалення після операції | `filedo E: test del` |
| `nodel` | Зберігати тестові файли | `filedo C: speed 100 nodel` |
| `short` | Тільки коротка видача | `filedo D: speed 100 short` |
| `max` | Максимальний розмір (10ГБ) | `filedo C: speed max` |
| `old` | Залишити найновіший як оригінал (для cd) | `filedo D: cd old del` |
| `new` | Залишити найстарший як оригінал (для cd) | `filedo E: cd new move F:` |
| `abc` | Залишити алфавітно останній (для cd) | `filedo C: cd abc` |
| `xyz` | Залишити алфавітно перший (для cd) | `filedo C: cd xyz list dups.lst` |

---

## GUI додаток

**FileDO GUI** (`filedo_win.exe`) - оболонка на VB.NET (Windows Forms): ліворуч перелік завдань, на кожне завдання своя нумерована сторінка - що обробляти, які параметри, далі перевірка та запуск - плюс сторінка **«Команда»**, експертний конструктор, що збере й запустить **будь-яку** команду FileDO. Що є у вікні:

- **5 мов інтерфейсу** (EN / RU / UA / DE / FR, запам'ятовується) та **панель допомоги**: що робить операція й прапорці, який буде підсумок, і приклад для цілі
- **Вибір цілі** (пристрій / папка / мережа / файл); для **пристрою** шлях стає **вибором літери диска**, а вибір системного диска пояснює перенаправлення у `%TEMP%\FileDO_Operations`
- **Усі операції** на сторінці «Команда», в одному списку: info, speed, test, fill, clean, cd (дублікати), усе сімейство copy (copy / fastcopy / synccopy / balanced / maxcopy / smartcopy / safecopy), wipe, compare, check, probe, recover, from, hist
- **Джерело та призначення** для copy і compare
- **Прапорці за контекстом** (max, del, nodel, short, hist, `--force` для wipe) та правила видалення для дублікатів/порівняння
- **Сторінка на завдання** у вікні за умовчанням - ємність, швидкість, відомості, перевірка пошкоджених файлів, пряма перевірка, відновлення, дублікати, порівняння, очищення, копіювання, заповнення, стирання і три завдання для секретних файлів - і на кожній усі опції, які приймає CLI, разом з усіма двадцятьма дев'ятьма прапорцями `check`
- **Живий попередній перегляд** у редагованому полі на сторінці «Команда» - правте вручну все, що не покрив конструктор
- Кнопки **Browse** для шляхів, **Copy command** у буфер і **RUN** у вікні консолі
- Знаходить `filedo.exe` поруч із собою або в `PATH` (працює і в пакеті Store, і в портативному zip)
- **Сторінка «Про програму»**: збірка, автор і посилання на сайт, вихідний код, трекер проблем, сторінку приватності та інші програми автора
- **Надіслати логи автору** - єдина кнопка на цій сторінці збирає знайдені на машині логи FileDO в один zip, відкриває теку з виділеним файлом, копіює шлях у буфер обміну і відкриває вашу поштову програму з уже заповненою адресою та темою. Zip ви спершу бачите самі, і нічого не йде, поки ви самі не надішлете лист

```powershell
filedo_win              # якщо в PATH (winget / Store)
filedo_win.exe          # поруч з filedo.exe у портативному zip
```

Сторінки **«Захист»** працюють із секретними файлами `.fd-sec` - зробити файл секретним, повернути оригінал або відкрити його без розпакування. Пароль приховано маскою, і він ніколи не потрапляє в командний рядок. Якщо передати вікну шлях до `.fd-sec` напряму, воно відкриється на сторінці цього контейнера (`filedo_win.exe C:\a\x.fd-sec`); подвійний клік у Провіднику вікно взагалі не задіює - він виконує **Unsecure and start** у консолі, як описано вище. «Історія», «Налаштування» і «Про програму» мають власні сторінки; попереднє вікно-конструктор команд виведено з ужитку, а командний рядок вручну пишуть на сторінці **«Команда»**.

**Їде всюди:** GUI входить у пакет winget, портативний zip, MSI і є плиткою пакета Microsoft Store (MSIX).

---

## Розширені можливості

### Пакетна обробка
Створіть `commands.txt`:
```text
# Перевірка кількох пристроїв
device C: info
device D: test del
device E: speed 100
folder C:\temp clean
```

Виконайте: `filedo from commands.txt`

Коди виходу (усі команди, крім контейнерних): **0 passed or done, 1 ran and found a defect, 2 could not be verified.**

### Відстеження історії
```bash
filedo hist              # Показати останні 10 операцій
filedo history           # Показати історію команд
# Історія автоматично ведеться для всіх операцій
```

### Підтримка переривання
```bash
# Всі довгі операції підтримують переривання Ctrl+C
# Елегантне скасування з очищенням
# Контекстне переривання в оптимальних точках
```

### Мережеві операції
```bash
# SMB ресурси та мережеві диски
filedo \\server\backup speed 100
filedo \\nas\storage test del
filedo network \\pc\share info
```

---

## Важливі зауваження

> **Виявлення підробної місткості**: Створює 100 файлів (по 1% місткості кожен) з **підтримкою контекстного переривання**. Використовує сучасні випадкові шаблони перевірки та оптимізоване управління буферами для надійного виявлення.

> **Покращене переривання**: Всі довгі операції підтримують **елегантне скасування Ctrl+C** з автоматичним очищенням. Контекстні перевірки переривання в оптимальних точках для миттєвої відповіді.

> **Безпечне видалення**: `fill <розмір> del` перезаписує вільний простір з оптимізованим управлінням буферами та контекстним записом для безпечного видалення даних.

> **Тестові файли**: Створює файли `FILL_*.tmp` та `speedtest_*.txt`. Використовуйте команду `clean` для їх автоматичного видалення.

> **Модульна архітектура**: Рефакторинг з окремими пакетами `capacitytest` та `fileduplicates` для кращої підтримки та розширюваності.

---

## Приклади за випадками використання

<details>
<summary><b>Перевірка автентичності USB/SD карти</b></summary>

```bash
# Швидкий тест з очищенням
filedo E: test del

# Детальний тест, зберегти файли для аналізу
filedo F: test

# Спочатку перевірити інформацію про диск
filedo E: info
```
</details>

<details>
<summary><b>Бенчмарк продуктивності</b></summary>

```bash
# Швидкий тест 100МБ
filedo C: speed 100 short

# Максимальний тест продуктивності (10ГБ)
filedo D: speed max

# Тест швидкості мережі
filedo \\server\backup speed 500
```
</details>

<details>
<summary><b>Безпечне видалення даних</b></summary>

```bash
# Заповнити 5ГБ потім безпечно видалити
filedo C: fill 5000 del

# Очистити наявні тестові файли
filedo D: clean

# Перед утилізацією диска
filedo E: fill max del
```
</details>

<details>
<summary><b>Пошук та управління дублікатами</b></summary>

```bash
# Знайти дублікати в поточній директорії
filedo . check-duplicates

# Знайти та видалити старі дублікати
filedo C: cd old del

# Знайти та перемістити нові дублікати в резервну копію
filedo E: cd new move E:\Backup

# Зберегти список дублікатів для подальшої обробки
filedo D: cd list duplicates.lst

# Обробити збережений список з конкретною дією
filedo cd from list duplicates.lst xyz del
```
</details>

---

## Технічні деталі

### Архітектура
- **Модульний дизайн**: Розділений на спеціалізовані пакети для кращої підтримки
- **Контекстні операції**: Всі довгі операції підтримують елегантне скасування
- **Уніфікований інтерфейс**: Спільний інтерфейс `Tester` для всіх типів сховищ
- **Оптимізація пам'яті**: Потокові операції з оптимізованим управлінням буферами
- **Кросплатформенність**: Основна підтримка Windows з портованою базою коду Go

### Структура пакетів
```
FileDO/
├── main.go                    # Точка входу програми
├── capacitytest/             # Модуль тестування місткості
│   ├── types.go              # Основні інтерфейси та типи
│   ├── test.go               # Основна логіка тестування
│   └── utils.go              # Утиліти та функції перевірки
├── fileduplicates/           # Управління дублікатами файлів
│   ├── types.go              # Інтерфейси виявлення дублікатів
│   ├── duplicates.go         # Основна логіка дублікатів
│   ├── duplicates_impl.go    # Деталі реалізації
│   └── worker.go             # Фонова обробка
├── filedo_win_vb/           # GUI програма VB.NET
│   ├── FileDOGUI.sln        # Рішення Visual Studio
│   ├── Program.vb           # Точка входу; вікно - ShellForm.vb
│   └── bin/                 # Скомпільований GUI виконуваний файл
├── command_handlers.go       # Обробка команд
├── device_windows.go         # Операції з пристроями
├── folder.go                 # Операції з папками
├── network_windows.go        # Операції з мережевими сховищами
├── interrupt.go              # Обробка переривань
├── progress.go               # Відстеження прогресу
├── main_types.go             # Застарілі визначення типів
├── history.json              # Історія операцій
└── hash_cache.json           # Кеш хешів для дублікатів
```

### Ключові функції
- **Покращений InterruptHandler**: Потокобезпечне переривання з підтримкою контексту
- **Оптимізоване управління буферами**: Динамічне змінення розмірів буферів для оптимальної продуктивності
- **Комплексне тестування**: Виявлення підробної місткості з випадковою верифікацією
- **Виявлення дублікатів**: Порівняння файлів на основі MD5 з кешуванням
- **Пакетна обробка**: Виконання скриптів з обробкою помилок
- **Ведення історії**: Відстеження операцій на основі JSON

---

## Історія версій

**v2607301014** (Поточна)
- **GUI**: інтерфейс 5 мовами (англійська, російська, українська, німецька, французька) з перемиканням мови на льоту та значком застосунку
- **GUI**: вікно "Про програму" з функцією надсилання логів автору
- **Приватність**: опубліковано сторінку конфіденційності, на неї посилаються сайт і сторінка в Store
- **CLI**: новий слоган "pleasantly paranoid" і зрозуміліший текст довідки
- **Документація**: перероблений сайт з новими покроковими посібниками (перевірка накопичувача, файли та копії, конструктор команд GUI)
- **Проєкт**: вихідні коди реорганізовано в `cmd/` (filedo, filedo-check, filedo-fill, filedo-test); розділено процеси збірки та релізу

**v2606120121** (Попередня)
- **Wipe**: посилено перевірки безпеки під час затирання
- **Дублікати**: виправлено коректність і паралелізм кешу дублікатів
- **Копіювання**: скорочено обходи каталогів під час копіювання

**v2605152056**
- **Інсталятор**: setup EXE (пакет WiX з MSI всередині) і окремий MSI; значок застосунку вбудовано в усі EXE та в запис у списку програм
- **Store**: специфікація публікації в Microsoft Store і зображення попереднього перегляду

**v2604272228**
- **Дистрибуція**: перший реліз у winget (SerZhyAle.FileDO) і релізний конвеєр GitHub Actions
- **Бінарники**: в усі бінарники вбудовано PE-версію та маніфест застосунку Windows
- **Документація**: сторінки GitHub Pages перероблено та спрощено

**v2507112115**
- **Велика рефакторизація**: Логіка тестування місткості винесена в виділений пакет `capacitytest`
- **Покращене переривання**: Додано контекстне скасування з потокобезпечним `InterruptHandler`
- **Покращена продуктивність**: Оптимізовані алгоритми управління буферами та верифікації
- **Краща архітектура**: Модульний дизайн з чітким поділом відповідальностей
- **VB.NET GUI**: Оновлена програма Windows Forms з кращою інтеграцією

**v2507082120**
- Додано виявлення та управління дублікатами файлів
- Множинні режими вибору дублікатів (old/new/abc/xyz)
- Кешування хешів для швидших сканувань дублікатів
- Підтримка збереження/завантаження списків дублікатів
- GUI програма з функціями управління дублікатами

**v2507062220** (Рання)
- Покращена система верифікації з багатопозиційною перевіркою
- Читабельні текстові шаблони для виявлення пошкоджень
- Покращене відображення прогресу та механізми захисту
- Виправлення помилок та покращення обробки помилок

---

<div align="center">

**FileDO v2607301014** - Розширені інструменти для файлів і сховищ

Створено **sza@ukr.net** | [Ліцензія MIT](LICENSE) | [GitHub репозиторій](https://github.com/SerZhyAle/FileDO) | [Universal Agent Kit](https://serzhyale.github.io/universal-agent-kit/)

---

### Останні покращення

- **Модульна архітектура**: Рефакторинг у спеціалізовані пакети (`capacitytest`, `fileduplicates`)
- **Покращене переривання**: Контекстне скасування з елегантним очищенням
- **Потокобезпечні операції**: Покращений `InterruptHandler` з захистом мьютексами
- **Краща продуктивність**: Оптимізовані алгоритми управління буферами та верифікації
- **Оновлений GUI**: Програма VB.NET Windows Forms з покращеною інтеграцією

</div>
