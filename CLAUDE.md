# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

**Read [`AGENTS.md`](AGENTS.md) - it is the single source of truth for this repo.** This file is a bare
pointer by design: it carries no guidance of its own, and nothing is duplicated into it. `AGENTS.md` covers
the shared SZA Unified Rules canon and how this repo consumes it, the project shape and frozen anchors, the
code map, which of the two scripts holds the tag, the testing gate and its exit codes, destructive-tool
safety, and the PR specifics. `RELEASE.md` is the long-form manual for both flows.

Prefer the in-repo skills `/build` and `/release` over running the scripts by hand.
